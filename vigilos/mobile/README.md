# Mobile (KMM)

Shared модуль: API client, модели (Camera, Event, User), репозитории с offline, usecases, DI (Koin).

Android: Jetpack Compose, ExoPlayer, события с push.

iOS: SwiftUI, AVPlayer, уведомления.

