# Local-video-server

Приложение для автоматического обнаружения видеокамер в локальной сети и получения информации о доступных видео потоках.

## Описание

Local-video-server - это кроссплатформенное приложение на Go, которое:
- Сканирует локальную сеть на наличие видеокамер
- Определяет поддерживаемые протоколы (RTSP, RTMP, HLS, WebRTC, ONVIF, MJPEG, MPEG-DASH, SIP, RTP/RTCP)
- Проверяет доступность RTSP каналов и получает их параметры настройки

## Документация

- **[ANALYSIS.md](ANALYSIS.md)** - Анализ языков программирования и протоколов видеонаблюдения
- **[DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)** - Детальный план разработки проекта

## Технологии

- **Язык:** Go (Golang)
- **Протоколы:** RTSP, RTMP, HLS, WebRTC, ONVIF, MJPEG, MPEG-DASH
- **Инструменты:** FFmpeg для анализа видео потоков

## Статус проекта

🚧 В разработке

## Планируемый функционал

- [x] Анализ технологий и протоколов
- [x] План разработки
- [x] Базовая инфраструктура
  - [x] Go модуль и структура проекта
  - [x] Система конфигурации
  - [x] Система логирования
  - [x] Модели данных (Device, Stream, Config)
  - [x] Утилиты для работы с сетью
- [ ] Сканирование сети
- [ ] Определение протоколов
- [ ] Проверка RTSP каналов
- [ ] Экспорт результатов

## Быстрый старт

### Требования

- Go 1.21 или выше
- FFmpeg (для проверки RTSP потоков, опционально)

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd Local-video-server

# Установить зависимости
go mod download
go mod tidy

# Собрать приложение
go build -o bin/local-video-server ./cmd/server

# Или использовать Makefile
make build
```

### Использование

```bash
# Инициализировать конфигурационный файл
./bin/local-video-server init

# Сканировать сеть
./bin/local-video-server scan

# Подробный вывод
./bin/local-video-server scan --verbose

# Использовать свой конфиг
./bin/local-video-server scan --config /path/to/config.yaml
```

## Структура проекта

```
Local-video-server/
├── cmd/server/          # Точка входа приложения
├── internal/            # Внутренние пакеты
│   ├── config/          # Система конфигурации
│   └── models/          # Модели данных
├── pkg/utils/           # Утилиты
│   ├── logger.go        # Логирование
│   └── network.go       # Работа с сетью
├── configs/             # Конфигурационные файлы
└── docs/                # Документация
```

## Лицензия

MIT 
